# Swa.Analyzers.Common

Source-only shared code for analyzer packages.

Files placed in this directory are included in analyzer projects with `Compile Include`.
This directory must not become a runtime assembly or NuGet dependency for consumers.
