### New Rules

Rule ID | Category    | Severity | Notes
--------|-------------|----------|------
TST001 | TestQuality | Disabled | Novo ID v2. Opt-in com severidade base Info; restringe `NSubstitute.Arg.Any()` e expande a cobertura para `ReturnsForAnyArgs`, `WhenForAnyArgs` e `ReceivedWithAnyArgs`, mantendo `DidNotReceiveWithAnyArgs` permitido.
TST002 | TestQuality | Disabled | Novo ID v2. Opt-in com severidade base Info; avisa sobre exclusoes do FluentAssertions em `BeEquivalentTo`.
