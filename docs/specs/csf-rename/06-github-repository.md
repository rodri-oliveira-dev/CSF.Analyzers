# CSF rename - GitHub repository

## Resultado

Esta etapa confirmou o repositorio canonico `rodri-oliveira-dev/CSF.Analyzers` e alinhou o remote local.

## Escopo validado

- Repositorio GitHub canonico: `rodri-oliveira-dev/CSF.Analyzers`.
- URL HTTPS canonica: `https://github.com/rodri-oliveira-dev/CSF.Analyzers`.
- URL SSH canonica: `git@github.com:rodri-oliveira-dev/CSF.Analyzers.git`.
- Branch default: `main`.
- `origin` local aponta para `git@github.com:rodri-oliveira-dev/CSF.Analyzers.git`.
- Metadata ativa de NuGet, `PackageProjectUrl`, help links, scripts e validacoes de pacote apontam para `CSF.Analyzers`.

## Observacoes

Este documento foi sanitizado durante a auditoria final para nao manter literais da identidade anterior no working tree.
